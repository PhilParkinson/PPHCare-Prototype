import 'package:flutter/material.dart';

class WebProfileScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Profile Page Now Open')),
    );
  }
}
